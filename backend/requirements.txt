# Core FastAPI dependencies
fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# HTTP and web scraping (FREE)
httpx==0.25.2
beautifulsoup4==4.12.2
requests==2.31.0

# FREE LLM APIs
google-generativeai==0.3.2
# Note: Deepseek and Perplexity use httpx directly (no special SDK needed)

# Background tasks (simple threading for free version)
# celery==5.3.4  # Not needed for free MVP
# redis==5.0.1   # Not needed for free MVP

# Data processing (optional for free MVP)
# pandas==2.1.3  # Not needed for basic functionality
# numpy==1.25.2  # Not needed for basic functionality

# Database migrations (optional for free MVP)
# alembic==1.12.1  # SQLite doesn't need complex migrations

# Advanced scraping (not needed for free version)
# selenium==4.15.2  # Heavy dependency, not needed
# scrapy==2.11.0    # Overkill for simple scraping

# OpenAI (not needed for free MVP)
# openai==1.3.5     # Using free alternatives instead
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0